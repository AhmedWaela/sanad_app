allprojects {
    repositories {
        google()
        mavenCentral()
    }
}

buildscript{
 dependencies{
    // It's like teeling the architect(Gradle) : "Use the lastest tools and techniques avaliable in verion 7.3.1 to build this house(App)"
    classpath 'com.android.tools.build:gradle:7.3.1'
 }
}

rootProject.buildDir = "../build"
subprojects {
    project.buildDir = "${rootProject.buildDir}/${project.name}"
}
subprojects {
    project.evaluationDependsOn(":app")
}

tasks.register("clean", Delete) {
    delete rootProject.buildDir
}
